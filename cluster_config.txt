# Cluster Configuration
# fill out the following:
# submission_cmd = e.g "bsub","qsub"
# submission_memory_flag = e.g. "-M","-q umem -M"
# memory_1 = has to at least 7,000 mb  
# memory_2 = has to at least 8,000 mb 

## LSF
## ----------------
submission_cmd = "bsub" 
submission_memory_flag = "-M"
memory_1 = "7000" #in mb
memory_2 = "8000" #in mb
## ----------------

